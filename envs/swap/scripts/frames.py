"""Named frame prim paths — must match envs/swap/dock.usda and SPEC.md."""

DOCK_ROOT = "/Dock"

APPROACH = f"{DOCK_ROOT}/frames/approach"
BERTH = f"{DOCK_ROOT}/frames/berth"
PACK_ATTACH = f"{DOCK_ROOT}/frames/pack_attach"
EMPTY_READY = f"{DOCK_ROOT}/frames/empty_ready"
FULL_STOW = f"{DOCK_ROOT}/frames/full_stow"

LIFT_JOINT = f"{DOCK_ROOT}/lift/lift_joint"
CARRIAGE = f"{DOCK_ROOT}/lift/carriage"

# Provisional until Dimitris publishes robot/SPEC.md
ROVER_BATTERY_MOUNT = "/Sheep/battery_mount"
